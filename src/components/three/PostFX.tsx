/**
 * PostFX — stub
 *
 * All background-tab effects (Glow FX, Color FX, Stylize) have been moved
 * into the BackgroundPlane shader so they only affect the background layer.
 *
 * This component is kept as a placeholder for any future scene-wide effects
 * that intentionally need to affect bars, particles, and logo as well.
 */
export function PostFX() {
  return null;
}
